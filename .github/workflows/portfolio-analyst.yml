# Workflow for Portfolio Analyst

name: Portfolio Analyst

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Previous steps...

      # Install gh-aw extension
      - name: Install gh-aw extension
        run: |
          # Your installation command here

      # Download logs from last 30 days
      - name: Download logs from last 30 days
        run: |
          # Command to download logs here

      # Subsequent steps...

      # Job conclusion
      - name: Conclusion
        run: |
          # Your conclusion command here
